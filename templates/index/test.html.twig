{% extends 'base.html.twig' %}

{% block body %}
    {% for sub in pagination %}
        {{ sub.title }}
    {% endfor %}
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}

{% block title %}

{% endblock %}

